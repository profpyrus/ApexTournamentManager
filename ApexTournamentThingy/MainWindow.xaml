<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ApexTournamentThingy"
        xmlns:Linq="clr-namespace:Newtonsoft.Json.Linq;assembly=Newtonsoft.Json" x:Class="ApexTournamentThingy.MainWindow"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        mc:Ignorable="d"
        Title="MainWindow" Height="433" Width="782">
    <Window.Resources>
        <DataTemplate x:Key="teamTabTemplate">
            <Grid x:Name="team1grid" Background="White" Margin="10,10,10,10">
                <TextBox x:Name="namebox" HorizontalAlignment="Left" Margin="78,4,0,0" Text="{Binding Path=name, Mode=TwoWay}" KeyDown="TextBox_KeyEnterUpdate" TextWrapping="Wrap" VerticalAlignment="Top" Width="200"/>
                <Label Content="Team Name" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <ListBox HorizontalAlignment="Left" VerticalAlignment="Top" ItemsSource="{Binding Path=players}" Margin="0,40,0,0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <Label Content="Player 1"/>
                                <TextBox x:Name="player2box" Text="{Binding Path=name, Mode=TwoWay}" TextWrapping="Wrap" Width="120"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <Button Content="Button" HorizontalAlignment="Left" Margin="0,0,0,0" VerticalAlignment="Bottom" Click="Button_Click"/>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="playerTemplate">
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <TabControl>
            <TabItem Header="Team Management">
                <Grid Background="White">
                    <TabControl x:Name="teamTabControl" ItemsSource="{Binding Path=session.teams}" ContentTemplate="{StaticResource ResourceKey=teamTabTemplate}" Margin="0,0,0,33">
                        <TabControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Path=name}">
                                </TextBlock>
                            </DataTemplate>
                        </TabControl.ItemTemplate>
                    </TabControl>
                    <Button Content="Button" HorizontalAlignment="Left" Margin="10,0,0,8" VerticalAlignment="Bottom" Click="Button_Click_1"/>
                </Grid>
            </TabItem>
            <TabItem Header="Leaderboards">
                <Grid Background="White"/>
            </TabItem>
            <TabItem Header="Settings">
                <Grid Background="White"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>